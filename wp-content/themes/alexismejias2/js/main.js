$().ready(function() {
/*	var content = $('#left');
		//offset= content.offset();
	$(window).on('resize load scroll ready', function(event){
		var h = $(window).height();
		$(content).css('height',(h-20)+'px');
		//console.log(offset);
	});	
	console.log(content);*/
});